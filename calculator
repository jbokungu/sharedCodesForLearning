# This is a simple Calculator which supports basics arithmetics
# opertions : +, -, * and /.

num1 = float(input("Enter the first numer: "))
op = input("Enter the operaror: ")
num2 = float(input("Enter the second number: "))

# This small code can work without using a function but only if statements. I personnaly chose to use
# a function. Just a preference
# calculator function for opertors +, -, * and /
def calculator(num1,op, num2):
    if op == "+":
        return num1 + num2
    elif op == "-":
        return num1 - num2
    elif op == "*":
        return num1 * num2
    elif op == "/":
        return num1 / num2
    else:
        print("Invalid operator")

# call of the fuction with valuers entered by the user
print(calculator(num1,op, num2))